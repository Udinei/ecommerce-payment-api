server:
  port: 8082

spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/payment
    username: admin
    password: admin
    driver-class-name: org.postgresql.Driver
    hikari:
      connection-test-query: select 1
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        #default_schema: payment
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        show_sql: true
        use_sql_comments: true
        jdbc:
          lob:
            non_contextual_creation: true
  cloud:
    stream:
      kafka: # brocker
        binder:
          autoCreateTopics: true
          brokers: localhost:9092 # porta padroo do kafka
          configuration:
            value:
              deserializer: io.confluent.kafka.serializers.KafkaAvroDeserializer
              serializer: io.confluent.kafka.serializers.KafkaAvroSerializer
            key:
              deserializer: io.confluent.kafka.serializers.KafkaAvroDeserializer
              serializer: io.confluent.kafka.serializers.KafkaAvroSerializer
      bindings:
        checkout-created-input:
          destination: streaming.ecommerce.checkout.created
          contentType: application/*+avro
          group: ${spring.application.name}
          consumer:
            use-native-encoding: true # use o da chave configuration: acima
        payment-paid-output:
          destination: streaming.ecommerce.payment.paid
          contentType: application/*+avro
          producer:
            use-native-decoding: true
  kafka:
    properties:
      schema:
        registry:
          url: http://localhost:8081
  application:
    name: payment-api